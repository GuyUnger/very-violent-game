[gd_scene load_steps=18 format=3 uid="uid://bs15p6iks3jtm"]

[ext_resource type="Script" uid="uid://cj7scnl02hmwg" path="res://stages/main.gd" id="1_0oqm6"]
[ext_resource type="PackedScene" uid="uid://n2142e5rpapd" path="res://game/player/player.tscn" id="2_43f5m"]
[ext_resource type="Script" uid="uid://8gh7wu8tphq3" path="res://stages/hud.gd" id="3_7iyax"]
[ext_resource type="Environment" uid="uid://dwy0mt1yukigj" path="res://content/environment/environment.tres" id="3_fejr8"]
[ext_resource type="AudioStream" uid="uid://lc5v3oph86at" path="res://game/count_down.wav" id="4_0oqm6"]
[ext_resource type="AudioStream" uid="uid://bi4e28jxmil2m" path="res://game/kill 01.wav" id="6_byaf2"]
[ext_resource type="AudioStream" uid="uid://bpy78hrxwsp4t" path="res://game/all_kills.wav" id="7_byaf2"]
[ext_resource type="Script" uid="uid://coahlfyq6hr5l" path="res://stages/preloader.gd" id="8_85ei1"]
[ext_resource type="PackedScene" uid="uid://bn61hhe8nalsi" path="res://game/projectiles/bullet.tscn" id="9_1k4el"]
[ext_resource type="PackedScene" uid="uid://dn8qcdsnyofpu" path="res://game/weapons/model/muzzleflash.tscn" id="9_lnxn1"]
[ext_resource type="PackedScene" uid="uid://cgyal7lg7rsgh" path="res://game/npc/blood_splash.tscn" id="10_lnxn1"]
[ext_resource type="PackedScene" uid="uid://biscewy3yvfw8" path="res://game/fx/blood_splat.tscn" id="11_v8qa1"]
[ext_resource type="PackedScene" uid="uid://fk2ws4nkdayk" path="res://game/weapons/weapon_smg.tscn" id="12_cvug1"]

[sub_resource type="Shader" id="Shader_0oqm6"]
code = "shader_type canvas_item;

uniform sampler2D SCREEN_TEXTURE : hint_screen_texture, filter_linear_mipmap;

uniform float fish_intensity = 1.0;
uniform float scale_correction : hint_range(0.9, 1.1) = 0.99;

void fragment() {
	vec2 uv = SCREEN_UV;
	uv -= 0.5;
	uv *= scale_correction;
	uv += 0.5;

	// Convert to lens-space (-1,1) and correct for aspect ratio
	float aspect_ratio = SCREEN_PIXEL_SIZE.y / SCREEN_PIXEL_SIZE.x;
	vec2 lens_pos = (uv - 0.5) * 2.0;
	lens_pos.x *= aspect_ratio;
	float lens_radius = length(lens_pos);

	// Equisolid-angle remap: screen radius -> source radius
	const float max_fov_deg = 70.0; // visible field of view
	float max_fov_rad = radians(max_fov_deg);
	float theta = lens_radius * max_fov_rad * fish_intensity;
	float src_radius = tan(theta * 0.5) / tan(max_fov_rad * 0.5);
	vec2 unit_dir = lens_pos / max(lens_radius, 1e-6);
	vec2 src_pos = unit_dir * src_radius;
	src_pos.x /= aspect_ratio;
	vec2 sample_uv = src_pos * 0.5 + 0.5; // back to 0,1

	float center_dist = length(sample_uv - 0.5);

	vec2 offset = vec2(0.003, 0.002) * length(uv - 0.5);
	vec3 color_l = vec3(0.6, 0.6, 0.0);
	vec3 color_r = vec3(0.0, 0.6, 0.6);
	vec3 color_c = vec3(1.0) - color_l - color_r;
	vec3 sample_c = texture(SCREEN_TEXTURE, sample_uv).rgb * color_c;
	vec3 sample_l = texture(SCREEN_TEXTURE, sample_uv - offset).rgb * color_l;
	vec3 sample_r = texture(SCREEN_TEXTURE, sample_uv + offset).rgb * color_r;

	vec3 sample = sample_c + sample_l + sample_r;
	// hue-shift deges
	COLOR.rgb = mix(sample, sample.bgr, pow(center_dist, 1.0) * 0.5);
}"

[sub_resource type="ShaderMaterial" id="ShaderMaterial_fejr8"]
shader = SubResource("Shader_0oqm6")
shader_parameter/fish_intensity = 0.8
shader_parameter/scale_correction = 0.961

[sub_resource type="Shader" id="Shader_fejr8"]
code = "shader_type canvas_item;
render_mode blend_mix;

uniform sampler2D screen_texture : hint_screen_texture;

// RGB -> HSV
vec3 rgb2hsv(vec3 c) {
    float cmax = max(c.r, max(c.g, c.b));
    float cmin = min(c.r, min(c.g, c.b));
    float delta = cmax - cmin;

    float h = 0.0;
    if (delta > 1e-5) {
        if (cmax == c.r) {
            h = mod(((c.g - c.b) / delta), 6.0);
        } else if (cmax == c.g) {
            h = ((c.b - c.r) / delta) + 2.0;
        } else {
            h = ((c.r - c.g) / delta) + 4.0;
        }
        h /= 6.0;
        if (h < 0.0) h += 1.0;
    }

    float s = (cmax <= 0.0) ? 0.0 : delta / cmax;
    float v = cmax;

    return vec3(h, s, v);
}

// HSV -> RGB
vec3 hsv2rgb(vec3 c) {
    float h = fract(c.x) * 6.0;
    float s = clamp(c.y, 0.0, 1.0);
    float v = clamp(c.z, 0.0, 1.0);

    int i = int(floor(h));
    float f = h - float(i);
    float p = v * (1.0 - s);
    float q = v * (1.0 - s * f);
    float t = v * (1.0 - s * (1.0 - f));

    if (i == 0) return vec3(v, t, p);
    if (i == 1) return vec3(q, v, p);
    if (i == 2) return vec3(p, v, t);
    if (i == 3) return vec3(p, q, v);
    if (i == 4) return vec3(t, p, v);
    return vec3(v, p, q);
}
float ease_in_out(float x, float amt) {
    x = clamp(x, 0.0, 1.0);
    if (amt <= 0.0) return x;
    float a = max(1.0, amt * 2.0); // strength
    float p = pow(x, a);
    float q = 1.0 - pow(1.0 - x, a);
    return mix(x, (p + q) * 0.5, amt);
}


void fragment() {
	float dither = 0.0;
	ivec2 pixel = ivec2(SCREEN_UV / SCREEN_PIXEL_SIZE);
	if (pixel % ivec2(4) == ivec2(0) || pixel % ivec2(4) == ivec2(2)) {
		dither = 1.0;
	}
	else if (pixel % ivec2(2) == ivec2(0) || pixel % ivec2(2) == ivec2(1)) {
		dither = 0.2;
	}
	float steps = mix(20.0, 4.0, dither);
	vec3 color = texture(screen_texture, SCREEN_UV).rgb;
	
	float red = color.r;
	color = rgb2hsv(color);
	color.g *= red + 0.3;
	color.b *= 0.8;
	color = hsv2rgb(color);

	color = round(color * steps) / steps;

	COLOR.rgb = color;
}"

[sub_resource type="ShaderMaterial" id="ShaderMaterial_5sjox"]
shader = SubResource("Shader_fejr8")

[node name="Main" type="Node3D"]
script = ExtResource("1_0oqm6")

[node name="CanvasLayer" type="CanvasLayer" parent="."]
layer = 2

[node name="Fisheye" type="ColorRect" parent="CanvasLayer"]
material = SubResource("ShaderMaterial_fejr8")
anchors_preset = 15
anchor_right = 1.0
anchor_bottom = 1.0
grow_horizontal = 2
grow_vertical = 2
mouse_filter = 2

[node name="CanvasLayer2" type="CanvasLayer" parent="."]
layer = 3

[node name="Dither" type="ColorRect" parent="CanvasLayer2"]
material = SubResource("ShaderMaterial_5sjox")
anchors_preset = 15
anchor_right = 1.0
anchor_bottom = 1.0
grow_horizontal = 2
grow_vertical = 2
mouse_filter = 2

[node name="SubViewportContainer" type="SubViewportContainer" parent="."]
anchors_preset = 15
anchor_right = 1.0
anchor_bottom = 1.0
grow_horizontal = 2
grow_vertical = 2

[node name="SubViewport" type="SubViewport" parent="SubViewportContainer"]
handle_input_locally = false
audio_listener_enable_3d = true
size = Vector2i(854, 480)
render_target_update_mode = 4

[node name="Player" parent="SubViewportContainer/SubViewport" instance=ExtResource("2_43f5m")]

[node name="WorldEnvironment" type="WorldEnvironment" parent="SubViewportContainer/SubViewport"]
environment = ExtResource("3_fejr8")

[node name="HUD" type="CanvasLayer" parent="."]
layer = 8
script = ExtResource("3_7iyax")

[node name="Time" type="Label" parent="HUD"]
unique_name_in_owner = true
anchors_preset = 5
anchor_left = 0.5
anchor_right = 0.5
offset_left = -20.5
offset_top = 10.0
offset_right = 20.5
offset_bottom = 55.0
grow_horizontal = 2
theme_override_colors/font_shadow_color = Color(0, 0, 0, 1)
theme_override_constants/shadow_offset_x = 0
theme_override_constants/shadow_offset_y = 2
theme_override_font_sizes/font_size = 32
text = "59"
horizontal_alignment = 1

[node name="Ammo" type="Label" parent="HUD"]
unique_name_in_owner = true
anchors_preset = 5
anchor_left = 0.5
anchor_right = 0.5
offset_left = -408.0
offset_top = 21.0
offset_right = -228.0
offset_bottom = 56.0
grow_horizontal = 2
theme_override_colors/font_shadow_color = Color(0, 0, 0, 1)
theme_override_constants/shadow_offset_x = 0
theme_override_constants/shadow_offset_y = 2
theme_override_font_sizes/font_size = 32
text = "Ammo: 59"

[node name="AudioCountDown" type="AudioStreamPlayer" parent="HUD"]
unique_name_in_owner = true
stream = ExtResource("4_0oqm6")

[node name="ProgressBar" type="ProgressBar" parent="HUD"]
unique_name_in_owner = true
self_modulate = Color(1, 0, 0, 1)
anchors_preset = 12
anchor_top = 1.0
anchor_right = 1.0
anchor_bottom = 1.0
offset_top = -27.0
grow_horizontal = 2
grow_vertical = 0
show_percentage = false

[node name="LabelEnemies" type="Label" parent="HUD/ProgressBar"]
unique_name_in_owner = true
layout_mode = 1
anchors_preset = 12
anchor_top = 1.0
anchor_right = 1.0
anchor_bottom = 1.0
offset_top = -18.0
grow_horizontal = 2
grow_vertical = 0
theme_override_font_sizes/font_size = 22
text = "0/5"
horizontal_alignment = 1

[node name="AudioKill" type="AudioStreamPlayer" parent="."]
unique_name_in_owner = true
stream = ExtResource("6_byaf2")
volume_db = -6.0

[node name="AudioKillComplete" type="AudioStreamPlayer" parent="."]
unique_name_in_owner = true
stream = ExtResource("7_byaf2")

[node name="Preloader" type="Node3D" parent="."]
transform = Transform3D(1, 0, 0, 0, 1, 0, 0, 0, 1, 0, -2, -9)
script = ExtResource("8_85ei1")

[node name="Bullet" parent="Preloader" instance=ExtResource("9_1k4el")]

[node name="Muzzleflash" parent="Preloader" instance=ExtResource("9_lnxn1")]

[node name="Node3D" parent="Preloader" instance=ExtResource("11_v8qa1")]

[node name="WeaponSmg" parent="Preloader" instance=ExtResource("12_cvug1")]

[node name="Node3D" parent="." instance=ExtResource("10_lnxn1")]
transform = Transform3D(1, 0, 0, 0, 1, 0, 0, 0, 1, -1, 0, 1)
